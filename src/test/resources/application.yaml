server:
  port: 8888

spring:
  application:
    name: techstock-manager

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: test-secret-123
  expiration: 3600000